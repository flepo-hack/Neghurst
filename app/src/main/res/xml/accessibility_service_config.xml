<?xml version="1.0" encoding="utf-8"?>
<!--
  `accessibilityEventTypes` requests ONLY `typeWindowStateChanged`.

  The previous config asked for `typeWindowStateChanged|typeWindowContentChanged`.
  A running game emits `typeWindowContentChanged` continuously as its HUD
  updates, which floods the service with thousands of events per second. That is
  a well known cause of AccessibilityService ANRs and, more importantly here, of
  gesture dispatch being delayed by tens of milliseconds on exactly the frame
  where a dodge has to be delivered.

  `typeWindowStateChanged` fires only when a window gains or loses focus, a few
  times per second at worst, and it is what lets Rendera know whether the game is
  actually in the foreground before spending a gesture. Running detection over the
  launcher or a menu would feed the tracker pure noise.

  `canRetrieveWindowContent` is off: nothing reads window content, so requesting
  it adds latency and review friction for no benefit.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity="com.example.MainActivity" />
